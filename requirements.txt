# A versão do PyPI (3.0.2) só aceita os filtros max_stops e airlines; preço
# máximo, bagagens, janela de horário, duração e afins só existem no
# repositório. O backend funciona com as duas — veja o README.
fast-flights @ git+https://github.com/AWeirdDev/flights.git

fastapi>=0.115
uvicorn[standard]>=0.30
tzdata; sys_platform == "win32"
